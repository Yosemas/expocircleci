version: 2.1

jobs:
  saludo:
      docker:
       - image: cimg/base:stable
      steps:
         - run: echo Hola
  despedida:
        docker:
         - image: cimg/base:stable
        steps:
          - run: echo Adios

workflows:
    proceso:
         jobs:
           - saludo
           - despedida:
              requires:
                - saludo
